## Template for TypeScript GraphQL Microservice

### Steps

1. Clone this template repository
2. Remove .git folder
3. Update packages.json and `environment/config.json`

### Port Settings

Port is configured in `config.json` and `docker-compose.yaml`
